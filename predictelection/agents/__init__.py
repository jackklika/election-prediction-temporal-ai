"""pydantic-ai agents.

Submodules are not re-exported: importing one constructs a model client and so
needs an API key, which nothing that merely handles contracts or writes rows
should require.
"""
